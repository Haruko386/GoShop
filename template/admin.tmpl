<!doctype html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>ColdMart · 发布商品</title>
    <link rel="stylesheet" href="/static/css/index.css">
</head>
<body>
<header class="topbar">
    <div class="wrap">
        <div class="row" style="justify-content:space-between;">
            <a class="brand" href="/"><div class="logo">CM</div><div><h1>ColdMart</h1><small>管理员 · 发布商品</small></div></a>
            <div class="row">
                <a class="pill" href="/">返回首页</a>
            </div>
        </div>
    </div>
</header>

<main class="wrap" style="padding-top:18px; padding-bottom:30px;">
    <section class="panel">
        <div class="panelHeader">
            <h3>发布商品（仅 ID=1 可用）</h3>
            <div class="muted small" id="statusText">—</div>
        </div>

        <div class="panelBody">
            <form id="stockForm" enctype="multipart/form-data">
                <div class="grid2">
                    <div class="field">
                        <label>商品名</label>
                        <input name="name" required />
                    </div>
                    <div class="field">
                        <label>分类</label>
                        <input name="category" placeholder="数码/服饰/..." />
                    </div>
                </div>

                <div class="grid2">
                    <div class="field">
                        <label>价格（整数）</label>
                        <input name="price" type="number" min="1" required />
                    </div>
                    <div class="field">
                        <label>库存</label>
                        <input name="inventory" type="number" min="0" required />
                    </div>
                </div>

                <div class="field">
                    <label>商品简介</label>
                    <input name="info" placeholder="一句话介绍" />
                </div>

                <div class="field">
                    <label>商品图片（可选）</label>
                    <input name="picture" type="file" accept="image/*" />
                </div>

                <div class="btnRow">
                    <button class="btn solid" type="submit">发布</button>
                </div>
            </form>
        </div>
    </section>

    <div class="toast" id="toast"></div>
</main>

<script type="module" src="/static/js/admin.js"></script>
</body>
</html>
