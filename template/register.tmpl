<!doctype html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>ColdMart · 注册</title>
    <link rel="stylesheet" href="../static/css/register.css">
</head>
<body>
<header class="topbar">
    <div class="wrap">
        <div class="row" style="justify-content:space-between;">
            <a class="brand" href="/">
                <div class="logo">CM</div>
                <div>
                    <h1>ColdMart</h1>
                    <small>简洁商城 · 注册</small>
                </div>
            </a>
            <a class="pill" href="/login">去登录</a>
        </div>
    </div>
</header>

<main>
    <section class="card">
        <div class="cardHead">
            <h2>注册</h2>
            <span style="color:var(--muted); font-size:12px;">Create account</span>
        </div>
        <div class="cardBody">
            <p class="desc">填写基本信息。对接后端时把提交地址替换为你的 gin 接口即可。</p>

            <form id="regForm">
                <div class="grid2">
                    <div class="field">
                        <label>用户名</label>
                        <input name="username" placeholder="例如：cold_user" autocomplete="username" required />
                    </div>
                    <div class="field">
                        <label>手机号（可选）</label>
                        <input name="phone_num" placeholder="例如：138****8888" autocomplete="tel" />
                    </div>
                </div>

                <div class="field">
                    <label>邮箱</label>
                    <input name="email" type="email" placeholder="例如：user@example.com" autocomplete="email" required />
                </div>

                <div class="grid2">
                    <div class="field">
                        <label>密码</label>
                        <input name="password" type="password" placeholder="至少 6 位" autocomplete="new-password" required />
                    </div>
                    <div class="field">
                        <label>确认密码</label>
                        <input name="password2" type="password" placeholder="再次输入密码" autocomplete="new-password" required />
                    </div>
                </div>

                <div class="hint">提示：这里只做前端校验；后端请再校验邮箱唯一性、密码强度等。</div>

                <div class="btnRow">
                    <button type="button" class="btn" onclick="location.href='/'">返回首页</button>
                    <button type="submit" class="btn solid">注册</button>
                </div>
            </form>
        </div>

        <div class="foot">
            <span>已有账号？<a class="link" href="/login">去登录</a></span>
        </div>
    </section>
</main>

<div class="toast" id="toast"></div>

<script type="module" src="/static/js/register.js"></script>
</body>
</html>
